version: '{branch} build {build}'
image: Visual Studio 2017

environment:
  APP_NAME: 漢字 kah Lô-má-jī

init:
  - ps: chcp
  - ps: echo "${env:APP_NAME}"
  - ps: . cmd.bat
  - ps: chcp 65001
  - ps: echo "${env:APP_NAME}"
  - ps: . cmd.bat

install:
  - ps: chcp
  - ps: echo "${env:APP_NAME}"
  - ps: . cmd.bat
  - ps: chcp 65001
  - ps: echo "${env:APP_NAME}"
  - ps: . cmd.bat
